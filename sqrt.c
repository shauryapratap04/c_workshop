#include<stdio.h>
#include<math.h>
#include<conio.h>
void main()
{
	float a,r;
	
	root:
		{
		r=sqrt(a);
	printf("sqrt is %f \n", r);
		
	}printf("Enter the number \t");
	scanf("%f", &a);
	if(a!=1)
	goto root;

	//printf("sqrt is %f", root);
	
}